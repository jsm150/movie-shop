# 커밋 메시지 규칙

다음 형식으로 커밋 메시지를 작성한다.

`Type(scope): 한글 메시지`

작성 규칙:

1. `Type`는 앞글자만 대문자로 작성한다.
2. `Type`의 나머지 글자는 소문자로 작성한다.
3. `scope`는 Conventional Commit 관례에 따라 선택적으로 사용한다.
4. `:` 뒤에는 공백 한 칸을 둔다.
5. 메시지는 한글로 작성한다(기술 식별자/용어는 영문 사용 가능).

예시:

- `Feat(movie): 영화 상태 변경 기능 추가`
- `Fix(screening): 상영 시간 중복 검증 오류 수정`
- `Refactor: 극장 활성화 로직 정리`

## 로컬 설정 (권장)

1. 커밋 템플릿 설정:
   - `git config commit.template .gitmessage.txt`
2. `commit-msg` 훅 설치:
   - `ln -sf ../../scripts/validate-commit-msg.sh .git/hooks/commit-msg`

## 도메인 규칙 경계

모든 변경 사항에 아래 저장소 규칙을 적용한다.

- 도메인 규칙/정책은 반드시 도메인 계층에 선언한다.
- 절대로 핸들러/인프라 계층에서 규칙을 구현하지 않는다.
- `*.domain..`은 타 도메인 `*.domain..`에 직접 의존하지 않는다.
- 교차 도메인 연동은 소비 도메인이 포트를 선언하고, 소비 도메인 인프라 어댑터가 구현한다.

PR 리뷰 체크리스트:

- 핸들러/인프라에 상태 전이 분기(`if`, `switch`) 또는 비즈니스 판단이 없는가
- 도메인 aggregate/policy가 상태 전이 및 정책 검증 규칙을 소유하는가
- 도메인 예외는 도메인 계층에서 발생하는가

## 도메인 경계 설계 체크리스트

PR 작성 시 아래 항목을 반드시 확인한다.

- `domain`에서 타 도메인 `domain` import가 없는가
- 핸들러/인프라에 비즈니스 판단(`if`, `switch`)이 없는가
- 소비 도메인에 포트가 선언되어 있는가
- 포트 구현이 인프라에 있고 도메인 예외는 도메인에서 발생하는가

리뷰어 의사결정 규칙:

- 위반 발견 시 nit가 아닌 수정 필수로 처리한다.

레퍼런스(이번 구현 사례):

- Movie 삭제 정책 = Movie 도메인 포트 + Movie 인프라 어댑터

## 문서 기반 검증 시나리오

신규 기능 설계 리뷰 시 PR 설명에 아래 4줄을 필수로 포함한다.

- 소비 도메인 포트 정의 여부
- 도메인 정책에서 포트 사용 여부
- 인프라 어댑터 구현 위치 적합성
- 타 도메인 `domain` 직접 참조 부재
